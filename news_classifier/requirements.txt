# News Classifier - Python Dependencies
# Traditional ML only - NO deep learning!

# Core (already in Python stdlib, no install needed):
# - math, re, pickle, os, collections, string

# Minimal external dependencies:
numpy>=1.21.0

# Backend API (only if using backend):
flask>=2.0.0
flask-cors>=3.0.10
numpy>=1.21.0
pandas
scikit-learn
joblib
streamlit
flask>=2.0.0
flask-cors>=3.0.10